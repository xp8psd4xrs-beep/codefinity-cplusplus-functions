#include <iostream>

int calculateTax(double salary) {
    double taxRate = 0.18;
    double tax = salary * taxRate;
    return tax;
}

int main() {
    double salary = 2500.0;
    std::cout << "Tax for $" << salary << ": $" 
              << calculateTax(salary) << std::endl;
    return 0;
}